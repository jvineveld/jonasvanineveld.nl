import React from 'react'
import Header from '../components/header'

class JobTypes extends React.Component {

	render(){ return (
		<div className="main job-types">
			JobTypes
			<Header />
		</div>
	)}
}

export default (JobTypes)
